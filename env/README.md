# env

Local environment files live here and are ignored by git.

Expected examples, only when needed:

- `gemini.env` with `GEMINI_API_KEY=...`
- `openai.env` with `OPENAI_API_KEY=...`

Do not commit secrets or paste them into logs, prompts, reports, or run
manifests.

