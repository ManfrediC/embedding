# data/interim

Generated manifests, chunks, and draft datasets. Contents are ignored by git.

Use one subdirectory per operation so outputs remain easy to inspect, for
example:

- `manifest/`: full and pilot manifests.
- `text_views/`: paper and chunk text views.
- `embedding_runs/<run_id>/`: run-specific embedding inputs when needed.
