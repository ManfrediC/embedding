# tests

Focused tests for manifest construction, chunk provenance, and retrieval
evaluation helpers.

The first useful tests should check that generated chunks always keep
`paper_id`, source path, chunk id, text hash, and page/location metadata.

