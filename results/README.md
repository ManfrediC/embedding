# results

Run summaries, metrics, logs, and reports. Contents are ignored by git except
this README.

Use one subdirectory per operation, mirroring `data/interim/` where practical.
For example, manifest summaries belong under `manifest/`.
